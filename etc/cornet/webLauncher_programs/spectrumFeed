#!/bin/bash

set -ex

echo "LAUNCH_ID=$LAUNCH_ID"
echo "SERVER_ADDRESS=$SERVER_ADDRESS"

cd $(dirname ${BASH_SOURCE[0]})

spectrumFeed="crts_spectrumFeed"


exec $spectrumFeed --address $SERVER_ADDRESS $*
